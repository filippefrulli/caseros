NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ── Admin ───
ADMIN_EMAIL="filippefrulli.dev@gmail.com"
RESEND_FROM=""
RESEND_API_KEY=""

# ── Database 

#LOCAL
DATABASE_URL=""
DIRECT_URL=""

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

#PROD
#DATABASE_URL=""
#DIRECT_URL=""

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

# ── Supabase 
# Supabase → Settings → API Keys
#
# Project URL (same as before)
NEXT_PUBLIC_SUPABASE_URL=""
#
# Publishable key — replaces the legacy "anon" key. Format: sb_publishable_...
# Safe to expose in the browser. Enforces Row Level Security.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
#
# Secret key — replaces the legacy "service_role" key. Format: sb_secret_...
# Server-only. Bypasses RLS. Never expose to the client.
SUPABASE_SECRET_KEY=""
#
RESEND_API_KEY=""

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

STRIPE_SECRET_KEY=""

STRIPE_WEBHOOK_SECRET=""

SHIPPO_API_KEY=""

# ── Sendcloud (alternative shipping provider) ───────────────────────────────
# Panel → Settings → Integrations → Sendcloud API. Both keys required.
SENDCLOUD_PUBLIC_KEY=""
SENDCLOUD_PRIVATE_KEY=""
# Active provider: "shippo" (default) or "sendcloud".
SHIPPING_PROVIDER="shippo"

CRON_SECRET=""

# ── Kill switch ─────────────────────────────────────────────────────────────
# Set to "true" to take the app offline and show the maintenance page.
MAINTENANCE_MODE="false"